# /usr/bin/env python3
import numpy as np 

def trig():
    z= np.random.randint(0,10,size=(3,4))
    x= np.cos(z)
    x=np.sin(z)
    x=np.tan(z)
    x=np.acos(z)
    x=np.asin(z)
    x=np.atan(z)
    #Exponentes y logaritmos
    x=np.exp(z)
    x=np.exp2(z)
    x=np.power(z,4)
    ##logaritmos
    x=np.log(z)
    x=np.log2(z)
    x=np.log10(z)
    
    